python-telegram-bot[job-queue]>=21.11
Pillow>=10.4
python-dotenv>=1.0
# zoneinfo fallback data — python:*-slim images have no system tz database,
# so ZoneInfo('Europe/Berlin') would fail without this.
tzdata>=2024.1
