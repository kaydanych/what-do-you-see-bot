# Telegram bot token from @BotFather
BOT_TOKEN=

# Your Telegram user id (find via @userinfobot). Comma-separated for several admins.
ADMIN_IDS=

TZ=Europe/Berlin

# Where DB, photos and logs live. In Docker this is /data (see docker-compose.yml).
DATA_DIR=./data
